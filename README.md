To install and run:
``` bash
# installation
git clone https://github.com/kavonrtep/shinyGenome.git
cd shinyGenome
conda create -n shinyGenome -c conda-forge -c bioconda --file requirements.txt
conda activate shinyGenome
# run
./app.R
```
